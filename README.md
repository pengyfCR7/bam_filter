# bam_filter

This repository is currently empty apart from this README. The
`zhanglab-server_version` branch is reserved for direct, local execution on
`zhanglab-server`.

Add the filtering code and a concrete run command when the workflow is ready.
